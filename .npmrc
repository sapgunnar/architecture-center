# Security: Ignore lifecycle scripts in CI to prevent supply chain attacks
# Scripts like preinstall, postinstall can execute malicious code
# This is especially important for CI/CD environments
ignore-scripts=false

# For CI environments, set NPMRC_IGNORE_SCRIPTS=true to override
# Example: echo "ignore-scripts=true" >> .npmrc && npm ci
